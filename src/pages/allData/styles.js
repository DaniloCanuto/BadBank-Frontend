import styled from "styled-components";

const StyledAllData = styled.div`

  

  table {
    width: 100%;

    thead {
      background-color: var(--dark);
    }
    th {
      color: var(--light);
    }

    th,
    td {
      border: 1px solid var(--light);
      text-align: center;
    }
  }
`;

export default StyledAllData;
